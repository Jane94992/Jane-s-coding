#include <bits/stdc++.h>
using namespace std;
using ll=long long;
const ll M=998244353;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	int T;
	cin>>T;
	
	while(T--){
		int n,m,c,a,b;
		cin>>n>>m>>c>>a>>b;
		double dj=a/b;
		double ans;
		if(n>m) ans=0;
		if()
	}
	
	return 0;
}
