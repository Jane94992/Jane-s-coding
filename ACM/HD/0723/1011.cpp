#include <bits/stdc++.h>
using namespace std;
using ll=long long;


int main(){
	ios::sync_with_stdio(false);
	cin.tie(0);
	int T;
	cin>>T;
	for(T--){
		int n;
		cin>>n;
		ll sum=0;
		vector<int> a(n+2,0);
		for(int i=1;i<=n;i++){
			cin>>a[i];
		}
		
	}
	return 0;
}
